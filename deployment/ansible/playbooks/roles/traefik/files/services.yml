http:
  routers:
    # ***ARANGO_DB_NAME***-qa:
    #   entryPoints:
    #     - websecure
    #   rule: "Host(`qa.***ARANGO_DB_NAME***.bio`)"
    #   service: ***ARANGO_DB_NAME***-qa-service
    #   tls:
    #     certResolver: "le"
    #     domains:
    #       - main: "qa.***ARANGO_DB_NAME***.bio"
    # ***ARANGO_DB_NAME***-staging:
    #   entryPoints:
    #     - websecure
    #   rule: "Host(`staging.***ARANGO_DB_NAME***.bio`)"
    #   service: ***ARANGO_DB_NAME***-staging-service
    #   tls:
    #     certResolver: "le"
    #     domains:
    #       - main: "staging.***ARANGO_DB_NAME***.bio"
    # ***ARANGO_DB_NAME***-prod:
    #   entryPoints:
    #     - websecure
    #   rule: "Host(`***ARANGO_DB_NAME***.bio`)"
    #   service: ***ARANGO_DB_NAME***-prod-service
    #   tls:
    #     certResolver: "le"
    #     domains:
    #       - main: "***ARANGO_DB_NAME***.bio"
    # ***ARANGO_DB_NAME***-demo:
    #   entryPoints:
    #     - websecure
    #   rule: "Host(`demo.***ARANGO_DB_NAME***.bio`)"
    #   service: ***ARANGO_DB_NAME***-demo-service
    #   tls:
    #     certResolver: "le"
    #     domains:
    #       - main: "demo.***ARANGO_DB_NAME***.bio"
    ***ARANGO_DB_NAME***-test:
      entryPoints:
        - websecure
      rule: "Host(`test.***ARANGO_DB_NAME***.bio`)"
      service: ***ARANGO_DB_NAME***-test-service
      tls:
        certResolver: "le"
        domains:
          - main: "test.***ARANGO_DB_NAME***.bio"

  services:
    # ***ARANGO_DB_NAME***-qa-service:
    #   loadBalancer:
    #     servers:
    #       - url: "http://10.0.100.5"
    # ***ARANGO_DB_NAME***-staging-service:
    #   loadBalancer:
    #     servers:
    #       - url: "http://10.0.100.7"
    # ***ARANGO_DB_NAME***-prod-service:
    #   loadBalancer:
    #     servers:
    #       - url: "http://10.0.100.8"
    # ***ARANGO_DB_NAME***-demo-service:
    #   loadBalancer:
    #     servers:
    #       - url: "http://10.0.100.10"
    ***ARANGO_DB_NAME***-test-service:
      loadBalancer:
        servers:
          - url: "http://10.0.100.9"

