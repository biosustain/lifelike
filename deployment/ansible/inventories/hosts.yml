all:
    children:
        elk:
            hosts:
                elk.prod.***ARANGO_DB_NAME***.bio:
        webservers:
            hosts:
                demo.***ARANGO_DB_NAME***.bio:
                staging.***ARANGO_DB_NAME***.bio:
                qa.***ARANGO_DB_NAME***.bio:
        qa:
            hosts:
                qa.***ARANGO_DB_NAME***.bio:
        demo:
            hosts:
                demo.***ARANGO_DB_NAME***.bio:
        staging:
            hosts:
                staging.***ARANGO_DB_NAME***.bio:
        prod:
            hosts:
                ***ARANGO_DB_NAME***.bio:
