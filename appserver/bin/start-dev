#!/usr/bin/env bash

echo ">> Running in ${FLASK_ENV}"

__dir__="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"

# setup db
${__dir__}/dev-db-setup

# Start in server directory
cd "${__dir__}/.."

# Mark ready
touch .READY

echo "Running development server"
flask run --host 0.0.0.0
