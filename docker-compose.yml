version: '3.8'

services:
    appserver:
        image: ${REGISTRY:-gcr.io/able-goods-221820}/kg-appserver:${GITHUB_HASH:-latest}

    redis:
        container_name: redis
        image: redis:6.0.3-alpine

    webserver:
        image: ${REGISTRY:-gcr.io/able-goods-221820}/kg-webserver:${GITHUB_HASH:-latest}

    pdfparser:
        image: ${REGISTRY:-gcr.io/able-goods-221820}/kg-pdfparser:latest
