version: '3.8'

services:
    appserver:
        image: ${REGISTRY:-***ARANGO_DB_NAME***.azurecr.io}/kg-appserver:${GITHUB_HASH:-latest}

    webserver:
        image: ${REGISTRY:-***ARANGO_DB_NAME***.azurecr.io}/kg-webserver:${GITHUB_HASH:-latest}

    liquibase:
        image: ${REGISTRY:-***ARANGO_DB_NAME***.azurecr.io}/kg-liquibase:${GITHUB_HASH:-latest}

    pdfparser:
        image: ${REGISTRY:-***ARANGO_DB_NAME***.azurecr.io}/kg-pdfparser:latest

    filebeat:
        image: ${REGISTRY:-***ARANGO_DB_NAME***.azurecr.io}/filebeat:${GITHUB_HASH:-latest}

    cache-invalidator:
        image: ${REGISTRY:-***ARANGO_DB_NAME***.azurecr.io}/kg-cache-invalidator:${GITHUB_HASH:-latest}

    statistical-enrichment:
        image: ${REGISTRY:-***ARANGO_DB_NAME***.azurecr.io}/kg-statistical-enrichment:${GITHUB_HASH:-latest}
