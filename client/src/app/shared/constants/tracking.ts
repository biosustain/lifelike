/**
 * Maintain enum to avoid misspelling
 */
export enum TRACKING_CATEGORIES {
  workbench = 'workbench',
  filesystem = 'filesystem',
  sankey = 'sankey',
  visualiser = 'visualiser',
  search = 'search',
}

/**
 * Maintain enum to avoid misspelling
 */
export enum TRACKING_ACTIONS {
  activeTabChanged = 'active_tab_changed',
  navigateWithin = 'navigate_within',
  openedFile = 'opened_file',
  search = 'search',
  openTerm = 'open_term',
  expandNode = 'expand_node',
}
