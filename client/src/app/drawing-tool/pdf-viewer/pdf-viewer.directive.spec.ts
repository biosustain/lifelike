import { PdfViewerDirective } from './pdf-viewer.directive';

describe('PdfViewerDirective', () => {
  it('should create an instance', () => {
    const directive = new PdfViewerDirective(null);
    expect(directive).toBeTruthy();
  });
});
